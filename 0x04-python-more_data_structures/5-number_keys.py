#!/usr/bin/python3

def number_keys(a_dictionary):
    """
    Return the number of keys in a dictionary.
    """
    # Get the list of keys in the dictionary and calculate the length
    num_keys = len(a_dictionary.keys())

    return num_keys
